sources:
  - id: team_schedule
    type: example
    details:
      api_key: "<api_key>"
      schedule_id: "<schedule_id>"

destinations:
  - id: team_group
    type: example
    details:
      token: "<token>"
      group_id: "<group_id>"

syncs:
  - id: sync_schedule_to_group
    destination_id: team_group
    source_id: team_schedule
